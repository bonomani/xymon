# Base packages (all variants)
build-essential
libssl-dev
libpcre3-dev
librrd-dev
libtirpc-dev

# Server-only packages (VARIANT != client)
libc-ares-dev

# LDAP packages (ENABLE_LDAP=ON)
libldap-dev

# Optional compiler packages (only needed when CI_COMPILER=clang)
clang
