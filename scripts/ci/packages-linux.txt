# Base packages (all variants)
build-essential
perl
fping
libssl-dev
libpcre3-dev
librrd-dev
libtirpc-dev

# Server-only packages (VARIANT != client)
libc-ares-dev

# LDAP packages (ENABLE_LDAP=ON)
libldap-dev
libldap2-dev

# Compiler packages (CI_COMPILER=clang or non-debian profile)
clang
