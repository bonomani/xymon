# Dependency index
#
# Source-of-truth model:
# - deps-targets.yaml: shared family/os/pkgmgr + profile mapping (single source)
# - deps-base.yaml: shared profile/runtime base
# - deps-overlays.yaml: variant overlays (client/localclient/server)
# - deps-{server,client,localclient}.yaml: lightweight variant selectors
# - deps-version-notes.yaml: shared version notes referenced by deps-base.yaml
#
# Strict v2 selector schema:
# - targets_file
# - base_file
# - overlay_file
# - overlay_variant

# Installer helpers default to VARIANT=server if not provided; valid variants are 'server', 'client', and 'localclient'.
#
# Families:
# - gh-debian: GitHub Actions Ubuntu runners (version: latest)
# - ubuntu: Ubuntu-specific installs (version: latest/local/20)
# - debian: local Debian/Ubuntu installs (version: local)
# - bsd: FreeBSD/NetBSD/OpenBSD (version: auto)
