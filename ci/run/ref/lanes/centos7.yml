defaults:
  _all:
    runs_on: ubuntu-22.04
    container_options: --platform linux/amd64
include:
- name_prefix: CentOS 7
  platform_id: centos-7
  cmake_bin: cmake3
  variants: [server, localclient, client]
