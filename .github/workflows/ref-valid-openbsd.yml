name: Reference Validation - OpenBSD

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run-family:
    uses: ./.github/workflows/ref-validate-select.yml
    with:
      family: openbsd
    secrets: inherit
