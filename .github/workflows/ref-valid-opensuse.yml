name: Reference Validation - openSUSE

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run-family:
    uses: ./.github/workflows/ref-validate-select.yml
    with:
      family: opensuse
    secrets: inherit
