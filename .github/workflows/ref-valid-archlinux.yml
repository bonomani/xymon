name: Reference Validation - Arch Linux

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run-family:
    uses: ./.github/workflows/ref-validate-select.yml
    with:
      family: archlinux
    secrets: inherit
