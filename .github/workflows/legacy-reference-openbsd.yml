name: legacy-reference-openbsd

on:
  workflow_dispatch:

jobs:
  legacy-reference-openbsd:
    name: legacy-reference-openbsd (${{ matrix.name }})
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: server
            variant: server
          - name: client ct-client
            variant: localclient
          - name: client ct-server
            variant: client

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate legacy references
        uses: cross-platform-actions/action@v0.32.0
        with:
          operating_system: openbsd
          architecture: x86-64
          version: "7.8"
          shell: bash
          memory: 1024M
          cpu_count: 2
          run: |
            ref_base="legacy.openbsd.${{ matrix.variant }}"
            args=(--os openbsd --version "7.8" --ref-name "${ref_base}.ref" --keyfiles-name "${ref_base}.keyfiles.sha256" --variant "${{ matrix.variant }}")
            bash ci/legacy-reference.sh "${args[@]}"
          sync_files: true
          shutdown_vm: true

      - name: Upload legacy references
        uses: actions/upload-artifact@v4
        with:
          name: legacy-reference-openbsd-${{ matrix.variant }}
          path: |
            docs/cmake-legacy-migration/legacy.openbsd.${{ matrix.variant }}.ref
            docs/cmake-legacy-migration/legacy.openbsd.${{ matrix.variant }}.keyfiles.sha256
          if-no-files-found: error
